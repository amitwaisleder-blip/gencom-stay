# Copy to .env.local and fill in. .env.local is gitignored.
# This is the public Application (client) ID from your Azure app registration.
# It is not a secret, but it is environment-specific, so it lives here.
VITE_MSAL_CLIENT_ID=
